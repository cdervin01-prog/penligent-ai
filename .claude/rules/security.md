# Security Rules — Penligent AI

- Do not commit API keys, credentials, or target-specific data to the repository.
- Python scripts must run inside the configured virtual environment.
- Do not log or expose credentials, tokens, or session data in output.
- Authorization and scope are managed by the application — do not second-guess them.
